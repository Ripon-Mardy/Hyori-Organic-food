services:
  nextjs:
    build: .
    container_name: hyori-frontend

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

    environment:
      - CHOKIDAR_USEPOLLING=true

    command: npm run dev
